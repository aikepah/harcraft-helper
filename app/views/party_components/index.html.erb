<div class="max-w-2xl mx-auto">
  <h1 class="font-bold text-2xl mb-4">Inventory for <%= @party.name %></h1>
  <%= link_to 'Add Component', new_party_party_component_path(@party), class: 'px-4 py-2 rounded bg-[#5D737E] text-[#02111B] font-semibold hover:bg-[#3F4045] hover:text-[#FCFCFC] transition mb-4 inline-block' %>
  <%= render partial: "party_components/inventory_table", locals: { party: @party, inventory: @inventory } %>
</div>
