<div class="max-w-lg mx-auto">
  <h1 class="font-bold text-2xl mb-4"><%= @party.name %></h1>
  <p class="mb-2"><strong>Notes:</strong> <%= @party.notes %></p>
  <div class="mb-4">
    <%= link_to 'Edit', edit_party_path(@party), class: 'btn btn-secondary' %>
    <%= link_to 'Inventory', party_party_components_path(@party), class: 'btn btn-info' %>
    <%= link_to 'Back to Parties', parties_path, class: 'btn btn-light' %>
  </div>
</div>
